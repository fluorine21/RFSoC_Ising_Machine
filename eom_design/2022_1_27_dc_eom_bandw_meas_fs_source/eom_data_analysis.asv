clear; close all; clc;

t = readtable("3MHz.csv");

plot(t{:,1}, t{:,2});




%peak list is [time, value]
function peak_list get_peaks(peak_in, thresh)

    for i = 2:max(size(peak_in))-1
        if(peak_in(i,2) > thresh && peak_in(i,2) > peak_in(i-1,2))
    end


end

